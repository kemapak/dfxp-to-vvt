const readFile = require('./util.js');

class Converter {

    constructor() {
    }

    read(fileLocation) {
        return readFile(fileLocation);
    }

    write() {

    }

    convert(inputText) {

    }
}

module.exports = Converter;
